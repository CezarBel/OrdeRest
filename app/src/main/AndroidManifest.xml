<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orderrest">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BenedictOrderActivity"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapPizzahutActivity"
            android:label="@string/title_activity_map_pizzahut" />
        <activity
            android:name=".MapJapanicaActivity"
            android:label="@string/title_activity_map_japanica" />
        <activity
            android:name=".MapCafecafeActivity"
            android:label="@string/title_activity_map_cafecafe" />
        <activity
            android:name=".MapLandwerActivity"
            android:label="@string/title_activity_map_landwer" />
        <activity
            android:name=".MapAromaActivity"
            android:label="@string/title_activity_map_aroma" />
        <activity
            android:name=".MapArchieActivity"
            android:label="@string/title_activity_map_archie" />
        <activity
            android:name=".MapHanasiActivity"
            android:label="@string/title_activity_map_hanasi" />
        <activity
            android:name=".MapSunsetActivity"
            android:label="@string/title_activity_map_sunset" />
        <activity
            android:name=".MapHamburgActivity"
            android:label="@string/title_activity_map_hamburg" />
        <activity
            android:name=".MapBenedictActivity"
            android:label="@string/title_activity_map_benedict" />
        <activity android:name=".PizzahutActivity" />
        <activity android:name=".JapanicaActivity" />
        <activity android:name=".CafecafeActivity" />
        <activity android:name=".LandwerActivity" />
        <activity android:name=".AromaActivity" />
        <activity android:name=".ArchieActivity" />
        <activity android:name=".Hanasi1Activity" />
        <activity android:name=".SunsetActivity" />
        <activity android:name=".HamburgActivity" />
        <activity android:name=".BenedictActivity" />
        <activity
            android:name=".AllRestView"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar" />
        <activity android:name=".NewRestActivity" />
        <activity android:name=".UserProfile" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>